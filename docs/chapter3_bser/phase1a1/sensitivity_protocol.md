# S1 sensitivity protocol

S1 uses the first 20 valid M20 states selected by fixed hash order. Seven one-factor-at-a-time configurations are evaluated: the frozen default and six bounded perturbations. Defaults are not tuned from results.
