# Statistical unit contract

Protocol requests (240), raw unique planning states (197), solved unique states (194), trajectories (60), and scenarios (20) are distinct levels. Mathematical properties and solvers run once per unique state. The primary thesis unit is profile × scenario_seed; traces and snapshots are averaged inside that unit.
