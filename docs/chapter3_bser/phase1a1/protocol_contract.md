# E1-v2 protocol contract

The environment horizon is 400. A snapshot at step k is captured after exactly k actions for k in 0, 10, 25, and 50. The 4 profiles × 5 scenarios × 3 traces × 4 steps produce 240 recorded requests. Valid=226; skipped=14; valid step-50=49; artificial time-limit skips=0. Duplicate observable states map to one planning-state hash.
